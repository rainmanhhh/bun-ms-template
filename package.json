{
  "name": "bun-server",
  "version": "0.0.0",
  "devDependencies": {
    "@antfu/eslint-config": "^4.11.0",
    "@openapi-generator-plus/typescript-express-passport-server-generator": "^2.13.0",
    "@types/bun": "^1.2.8",
    "eslint": "^9.24.0",
    "eslint-plugin-format": "^1.0.1",
    "glob": "^11.0.1",
    "openapi-generator-plus": "^2.20.0",
    "rimraf": "^6.0.1",
    "typescript": "~5.7.2",
    "typescript-json-schema": "^0.65.1"
  },
  "private": true,
  "scripts": {
    "prebuild": "bun --bun prebuild/parseModules.ts",
    "dev": "bun run prebuild && bun --bun src/index.ts",
    "build": "bun run prebuild && bun build ./src/index.ts --outdir ./dist --target bun",
    "preview": "bun --bun dist/index.js",
    "check": "bunx --bun tsc",
    "lint": "bunx --bun eslint",
    "configSchema": "bunx --bun typescript-json-schema tsconfig.json -i src/IAppConfig.d.ts -o config/schema.json IAppConfig",
    "parseApi": "bunx --bun rimraf ./src/generated/server && bunx --bun openapi-generator-plus -c ./openapi/plus.yml"
  },
  "type": "module",
  "dependencies": {
    "express": "^5.1.0",
    "p-defer": "^4.0.1",
    "passport": "^0.7.0",
    "pino": "^9.6.0",
    "pino-pretty": "^13.0.0"
  }
}
